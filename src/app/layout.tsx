import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hungle Realty — Saskatchewan Farmland & Real Estate",
  description:
    "Search Saskatchewan farmland, acreages, and residential properties. Browse MLS® listings across 280+ Rural Municipalities with Adam Hungle, licensed REALTOR®.",
  keywords: [
    "Saskatchewan farmland",
    "SK real estate",
    "farm for sale Saskatchewan",
    "acreage for sale",
    "rural municipality",
    "Hungle Realty",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
